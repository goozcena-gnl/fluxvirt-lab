apiVersion: cdi.kubevirt.io/v1beta1
kind: StorageProfile
metadata:
  name: local-path
spec:
  cloneStrategy: copy
  claimPropertySets:
    - accessModes:
        - ReadWriteOnce
      volumeMode: Filesystem

apiVersion: v1
kind: Service
metadata:
  name: container-demo
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: container-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30081
